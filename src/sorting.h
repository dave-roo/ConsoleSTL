#pragma once

//ctor
class sorting
{
	public: sorting();
	virtual ~sorting(); //A virtual function is a member function which is declared within a base class and is re-defined(Overriden) by a derived class.
	void runSorting();
};
#pragma once

//ctor
class permutation
{
public: permutation();
	  virtual ~permutation(); //A virtual function is a member function which is declared within a base class and is re-defined(Overriden) by a derived class.
	  void runPermutation();
};